"""
Static-site mode for User routes: all Django view functions removed.
Routes are now defined in `user/urls.py` using TemplateView to render templates under `templates/user/`.
"""

# Intentionally left minimal.
